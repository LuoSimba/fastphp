


10022 提供了一个无效的参数

    socket_listen() 一个尚未绑定地址的 socket

10048 通常每个套接字地址(协议/网络地址/端口)只允许使用一次。

    socket_connect() 时候，系统会为客户端自动分配端口号，如果
    没有空余的端口号，而与正在使用的其他端口号重复，就会报错。

10054 远程主机强迫关闭了一个现有的连接。

    socket_send();
    socket_recv();

10056 在一个已经连接的套接字上做了一个连接请求

    socket_connect(); // 连接成功
    socket_connect(); // 报错

10057 由于套接字没有连接并且(当使用一个 sendto 调用发送数据报套接字时)没有提供地址，
      发送或接收数据的请求没有被接收。

    socket_send();
    socket_recv();

10061 由于目标计算机积极拒绝，无法连接。
    
    客户端 socket_connect() 发起连接，而服务端的队列已经满了，
    此时客户端被立即拒绝，就会产生连接失败的错误。



